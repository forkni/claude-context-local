[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "claude-context-local"
version = "0.7.1"
description = "Local semantic code search for Claude Code (MCP) using EmbeddingGemma, FAISS, and AST/tree-sitter chunking. 100% local embeddings and indexing."
readme = "README.md"
requires-python = ">=3.11"
authors = [
    { name = "forkni" }
]
maintainers = [
    { name = "forkni" }
]
keywords = [
    "semantic-search",
    "code-search",
    "embeddings",
    "faiss",
    "mcp",
    "claude",
    "tree-sitter",
    "chunking",
    "python",
]
dependencies = [
    "einops>=0.8.0",  # Required for CodeRankEmbed model
    "faiss-cpu>=1.13.1",
    "FlagEmbedding>=1.3.0",
    "huggingface-hub>=0.36.0",
    "mcp>=1.25.0",
    "nltk>=3.8",
    "pandas>=2.0.0",  # Required by datasets (transitive dependency of sentence-transformers)
    "psutil>=7.1.3",
    "rank-bm25>=0.2.2",
    "torch>=2.6.0",
    "torchvision>=0.21.0",
    "torchaudio>=2.6.0",
    "rich>=14.2.0",
    "sentence-transformers>=5.2.0",
    "transformers>=4.57.3",  # Required for Qwen3-0.6B support
    "sqlitedict>=2.1.0",
    "tree-sitter>=0.20.0",
    "tree-sitter-c>=0.24.1",
    "tree-sitter-c-sharp>=0.23.1",
    "tree-sitter-cpp>=0.23.4",
    "tree-sitter-glsl>=0.1.0",
    "tree-sitter-go>=0.25.0",
    "tree-sitter-java>=0.23.5",
    "tree-sitter-javascript>=0.25.0",
    "tree-sitter-python>=0.23.6",
    "tree-sitter-rust>=0.24.0",
    "tree-sitter-typescript>=0.23.2",
]

classifiers = [
    "License :: OSI Approved :: Apache Software License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Operating System :: OS Independent",
    "Intended Audience :: Developers",
    "Topic :: Software Development :: Libraries",
]

[project.optional-dependencies]
test = [
    "pytest>=9.0.1",
    "pytest-asyncio>=1.3.0",
    "pytest-cov>=6.2.1",
    "pytest-mock>=3.14.1",
]
dev = [
    "black>=25.11.0",
    "isort>=6.1.0",
    "ruff>=0.14.10",
    "pip-audit>=2.10.0",
    "pip-licenses>=5.5.0",
    "pipdeptree>=2.24.0",
]

[project.urls]
Homepage = "https://github.com/forkni/claude-context-local"
Repository = "https://github.com/forkni/claude-context-local"
Documentation = "https://github.com/forkni/claude-context-local#readme"
Issues = "https://github.com/forkni/claude-context-local/issues"

[tool.setuptools.packages.find]
include = ["chunking*", "embeddings*", "mcp_server*", "search*", "merkle*"]

# Security: Minimum versions for CVE fixes
# Direct dependencies: Updated in dependencies list above
# Transitive dependencies: Installed automatically, documented here for tracking
# Last audit: 2026-01-01 (pip-audit - 0 vulnerabilities)
#
# Direct dependency security updates:
# mcp>=1.25.0        # Fixes CVE-2025-66416 (DNS rebinding vulnerability)
#
# Transitive dependency security updates:
# authlib>=1.6.5     # Fixes CVE-2025-59420, CVE-2025-61920, CVE-2025-62706
# pip>=25.3          # Fixes CVE-2025-8869 (tarfile path traversal)
# starlette>=0.49.1  # Fixes CVE (version: 0.47.3 → 0.49.1)
# uv>=0.9.6          # Fixes 2 CVEs (version: 0.8.21 → 0.9.6)
# werkzeug>=3.1.4    # Fixes CVE-2025-66221 (Windows device name handling)
#
# Verification: .venv/Scripts/pip-audit --format json | .venv/Scripts/python.exe tools/summarize_audit.py
#
# Transitive dependency version constraints (cannot upgrade):
# fsspec<=2025.10.0   # datasets 4.4.1 requires fsspec[http]<=2025.10.0
# sympy==1.13.1       # torch 2.6.0 requires sympy==1.13.1 (pinned)
# huggingface-hub<1.0 # transformers 4.57.3 requires huggingface-hub>=0.34.0,<1.0
# numpy<2.4.0         # Multiple packages constrain numpy (faiss, scipy, pandas, etc.)
# torch==2.6.0        # Pinned by torchaudio, torchvision exact version match
#
# Successfully updated dependencies (tested 2026-01-01):
# Low-risk tools: ruff (0.13.3→0.14.10), pip-audit (2.9.0→2.10.0), psutil (7.0.0→7.1.3), rich (14.1.0→14.2.0)
# ML-adjacent: scipy (1.16.2→1.16.3), scikit-learn (1.7.1→1.8.0), networkx (3.5→3.6.1)
# ML-adjacent: pydantic (2.11.7→2.12.5), starlette (0.47.2→0.50.0), uvicorn (0.35.0→0.38.0)
# ML-CORE: transformers (4.56.2→4.57.3), sentence-transformers (5.1.1→5.2.0)
# ML-CORE: safetensors (0.6.2→0.7.0), accelerate (1.10.1→1.12.0)
# All 1,259 tests passed, embedding sanity check: 1.000000 similarity (perfect)

[tool.uv]
index-url = "https://pypi.org/simple"

[[tool.uv.index]]
name = "pytorch-cu124"
url = "https://download.pytorch.org/whl/cu124"
explicit = true

# Legacy CUDA 11.8 index (fallback for older systems)
[[tool.uv.index]]
name = "pytorch-cu118"
url = "https://download.pytorch.org/whl/cu118"
explicit = true

[tool.uv.sources]
# CUDA 12.4 (recommended for RTX 4090 and newer GPUs)
torch = [
    { index = "pytorch-cu124", marker = "sys_platform == 'linux' or sys_platform == 'win32'" },
]
torchvision = [
    { index = "pytorch-cu124", marker = "sys_platform == 'linux' or sys_platform == 'win32'" },
]
torchaudio = [
    { index = "pytorch-cu124", marker = "sys_platform == 'linux' or sys_platform == 'win32'" },
]

# Code quality tools configuration
[tool.black]
line-length = 88
target-version = ["py311"]
exclude = '''
/(
    \.git
  | \.venv
  | _archive
  | build
  | dist
  | node_modules
)/
'''

[tool.isort]
profile = "black"
line_length = 88
skip_glob = ["_archive/*", ".venv/*"]
known_first_party = ["chunking", "embeddings", "mcp_server", "search", "merkle", "evaluation"]

[tool.ruff]
line-length = 88
target-version = "py311"
exclude = [
    ".git",
    ".venv",
    "_archive",
    "build",
    "dist",
    "__pycache__",
    "tests/test_data",  # Sample code for testing, not production code
    "*.cmd",  # Batch files
    "*.bat",  # Batch files
]

[tool.ruff.lint]
select = [
    "E",  # pycodestyle errors
    "W",  # pycodestyle warnings
    "F",  # pyflakes
    "I",  # isort
    "B",  # flake8-bugbear
    "C4", # flake8-comprehensions
]
ignore = [
    "E501",  # line too long (handled by black)
]

[tool.ruff.lint.per-file-ignores]
# Imports inside try-except blocks cause conflicts between ruff and isort
"tests/integration/test_mcp_functionality.py" = ["I001"]
"tests/integration/test_token_efficiency_workflow.py" = ["I001"]

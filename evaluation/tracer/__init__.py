"""Execution-witnessed call-graph ground truth (WS-B, ADR-0059).

Sub-modules: ``collector`` (profiler), ``pytest_callgraph`` (opt-in plugin),
``build`` (raw runs -> traced_callgraph.json), ``scoring`` (per-tier metrics).
"""
